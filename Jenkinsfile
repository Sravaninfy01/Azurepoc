pipeline {
   agent any

   stages {
      stage('Terraform init') {
         steps {
            
           sh 'echo "hello, maven"'

            echo "The branch is ${params.branch}"
           
             
         }
      }
      
   }
}
