pipeline {
   agent any

   stages {
      stage('Terraform init') {
         steps {
            
           sh 'echo "hello, maven"'

            sh "The branch is ${params.branch}"
           
             
         }
      }
      
   }
}
